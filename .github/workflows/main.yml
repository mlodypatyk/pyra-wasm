name: "emsdk"
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: mymindstorm/setup-emsdk@v14

      - name: Build wasm
        #run: emcc -lembind -o ${{ github.workspace }}/pyra.js ${{ github.workspace }}/pyra.cpp -s ALLOW_MEMORY_GROWTH
        run: ls ${{ github.workspace }}
